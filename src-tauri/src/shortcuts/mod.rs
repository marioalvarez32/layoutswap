//! Per-layout shortcuts and the Remote Desktop scheduled task.
//!
//! Both are written through generated scripts (ADR-0001), never by calling the shell API
//! from Rust. Out of scope for the first slice; the module exists so the layout is final.
