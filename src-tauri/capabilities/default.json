{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may call the app's own commands and the core window API.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
